import 'package:flutter/material.dart';

// ignore: camel_case_types
class On_add extends StatelessWidget {
  const On_add({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Form(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
